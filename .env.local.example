# Copy to .env.local (Create React App loads it automatically).
# Point the UI at your local Express API so merge images and IndexedDB caching hit localhost:3001.
REACT_APP_API_URL=http://localhost:3001
